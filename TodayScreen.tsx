import React from 'react';
import {
  StyleSheet,
  View,
  Text
} from 'react-native';

const TodayScreen = () => {
  return (
    <View>
      <Text>Today</Text>
    </View>
  );
};

const styles = StyleSheet.create({
});

export default TodayScreen;
