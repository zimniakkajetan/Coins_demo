import React from 'react';
import {
  StyleSheet,
  View,
  Text
} from 'react-native';

const CoinsScreen = () => {
  return (
    <View>
      <Text>Coins</Text>
    </View>
  );
};

const styles = StyleSheet.create({
});

export default CoinsScreen;
